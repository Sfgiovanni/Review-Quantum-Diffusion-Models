# Pinned runtime environment for reproducible runs.
#
# This is a direct-dependency lock. For a fully hash-pinned lock including all
# transitive dependencies, regenerate in your environment with either:
#     uv pip compile pyproject.toml -o requirements-lock.txt --generate-hashes
#     pip-compile --generate-hashes -o requirements-lock.txt pyproject.toml
# and commit the result together with the run manifest (see change #11).
#
# Python: 3.11 (see Dockerfile for the frozen interpreter).
requests==2.32.3
feedparser==6.0.11
pandas==2.2.2
pyarrow==17.0.0
openpyxl==3.1.5
PyYAML==6.0.2
tqdm==4.66.5
tenacity==9.0.0
rapidfuzz==3.9.7
pydantic==2.9.2
matplotlib==3.9.2
# dev / test
pytest==8.3.3
responses==0.25.3
ruff==0.6.9
mypy==1.11.2
nbformat==5.10.4
nbconvert==7.16.4
ipykernel==6.29.5
tabulate==0.9.0
