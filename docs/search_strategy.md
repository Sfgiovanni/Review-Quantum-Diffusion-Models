# Search Strategy

Search concepts, query strings, date ranges, source scopes, rate limits, and scoring weights are defined in `configs/search_config.yaml`. Each query has a stable `query_id`, conceptual description, arXiv syntax, Crossref syntax, rationale, and strategy label.
